{
  "name": "vrfsm",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "pages:dev": "npm run dev & wrangler pages dev --proxy 5173",
    "pages:deploy": "npm run build && wrangler pages deploy dist"
  },
  "dependencies": {
    "three": "^0.163.0"
  },
  "devDependencies": {
    "vite": "^8.0.7",
    "wrangler": "^4.81.0"
  }
}
